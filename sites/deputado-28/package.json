{
  "name": "deputado-28",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Campaign site for Deputado 28",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=deputado-28",
    "deploy:preview": "npm run build && npx wrangler pages deploy dist --project-name=deputado-28 --branch=preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.5",
    "vite": "^8.2.1"
  }
}
